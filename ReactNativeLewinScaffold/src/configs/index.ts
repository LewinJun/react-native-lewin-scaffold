// code push配置
import RNConfig from 'react-native-config'
export const config = {
    ENV: RNConfig.ENV,
    CODEPUSH_KEY_IOS: RNConfig.CODEPUSH_KEY_IOS,
    CODEPUSH_KEY_ANDROID: RNConfig.CODEPUSH_KEY_IOS
}